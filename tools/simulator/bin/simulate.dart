/// Entry point for the simulator.
///
/// Pulled forward from P4 to P0 deliberately: every ring ratio is unverifiable
/// until the friend graph exists, which is roughly two months after launch.
/// This is the only way to test the most important algorithm in the app before
/// it runs on people.
///
/// Populated in chunk 7.
void main(List<String> args) {
  throw UnimplementedError(
    'No population model yet. See docs/v3/03_MATCHMAKER.md.',
  );
}
