name: lint
description: >-
  The dependency-rule lint (SC-6). Enforces the architecture in
  01_ARCHITECTURE.md §4 mechanically, because a rule with no CI check is a wish.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^9.0.0
